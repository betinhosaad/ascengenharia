<%= form_for(@clima) do |f| %>
  <% if @clima.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@clima.errors.count, "error") %> prohibited this clima from being saved:</h2>

      <ul>
      <% @clima.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
